Reset Local Enviroment
chrome://net-internals/#sockets


git add .
git commit -m "insert_text"
git push heroku master
git push heroku master --force

git add . && git commit -m "insert_text" && git push heroku master
git add . && git commit -m "validation_worker" && git push heroku master && sleep 10 && heroku restart

heroku logs --tail


TROUBLESHOOTING WORKERS
heroku ps

heroku restart

heroku ps:restart image_worker
heroku ps:restart validation_worker

heroku ps:scale image_worker=1
heroku ps:scale validation_worker=1

heroku logs --app heroimages --ps image_worker -t
heroku logs --app heroimages --ps validation_worker -t

