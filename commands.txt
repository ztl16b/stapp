Reset Local Enviroment
chrome://net-internals/#sockets


git add .
git commit -m "insert_text"
git push heroku master
git push heroku master --force


git add . && git commit -m "insert_text" && git push stapp master
git add . && git commit -m "dupe_check" && git push stapp master && sleep 10 && heroku restart

heroku logs --tail




TROUBLESHOOTING WORKERS
heroku ps --app stapp

heroku restart

heroku ps:restart image_worker --app stapp
heroku ps:restart bytescale_worker --app stapp

heroku ps:scale image_worker=1 --app stapp
heroku ps:scale bytescale_worker=1 --app stapp


heroku logs --app stapp --ps image_worker -t 
heroku logs --app stapp --ps bytescale_worker -t
